packages:
  # dbt-utils: surrogate key generation (generate_surrogate_key),
  # date spine (date_spine), expression tests, pivot utilities.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
