# Files matched here are not parsed by the DAG processor.
#
# nyc311_pipeline.py is the SPECIFICATION for the deferred cloud deployment
# (ADR 008). It imports airflow.providers.databricks, which is deliberately not
# installed locally — there is no Databricks workspace for it to talk to, and
# its job IDs default to 0. Parsing it locally only produces a permanent import
# error in the UI, so it is excluded here rather than half-loaded.
#
# A real cloud deployment would delete this line and install
# apache-airflow-providers-databricks alongside the DAG.
nyc311_pipeline.py
